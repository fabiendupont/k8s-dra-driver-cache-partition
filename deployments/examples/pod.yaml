apiVersion: v1
kind: Pod
metadata:
  name: cache-workload
spec:
  containers:
    - name: workload
      image: busybox:latest
      command: ["sleep", "infinity"]
      resources:
        claims:
          - name: cache
  resourceClaims:
    - name: cache
      resourceClaimName: my-cache-partition
