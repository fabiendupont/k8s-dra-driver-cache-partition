apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: {{ include "dra-cache-partition.fullname" . }}-slices
  labels:
    {{- include "dra-cache-partition.labels" . | nindent 4 }}
spec:
  selectors:
    - cel:
        expression: "device.driver == '{{ .Values.driverName }}'"
